site_name: mkdocs-drawio-exporter example
plugins:
  - search
  - glightbox
  - drawio-exporter:
      embed_format: "{content}"
      drawio_args: [--no-sandbox, --disable-gpu]

nav:
  - Overview: README.md
